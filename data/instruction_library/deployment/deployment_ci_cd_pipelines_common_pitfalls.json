{
  "module_id": "deployment_ci_cd_pipelines_common_pitfalls",
  "name": "Ci Cd Pipelines Common Pitfalls",
  "domain": "deployment",
  "description": "This module lists frequently observed mistakes, bugs, and anti-patterns seen in everyday ci cd pipelines work, so they can be recognized and avoided during typical ci cd pipelines tasks.",
  "capabilities": [
    "anti-patterns",
    "cd",
    "ci",
    "ci cd pipelines",
    "common mistakes",
    "common pitfalls",
    "deployment",
    "failure modes",
    "pipelines",
    "pitfalls to avoid"
  ],
  "content": "This module lists frequently observed mistakes, bugs, and anti-patterns seen in everyday ci cd pipelines work, so they can be recognized and avoided during typical ci cd pipelines tasks.\n\nCORE GUIDELINES\n1. When ci cd pipelines guidance conflicts with a specific user instruction, defer to the user's explicit instruction and note the conflict.\n2. Measure before optimizing any ci cd pipelines bottleneck; do not guess at what is slow.\n3. Before starting ci cd pipelines work, confirm the scope and constraints so the approach matches the actual requirement.\n4. Keep ci cd pipelines outputs reproducible: fix seeds, record versions, and avoid hidden non-determinism where it matters.\n5. State assumptions about ci cd pipelines explicitly before proceeding, rather than leaving them implicit.\n6. Validate ci cd pipelines outputs against a concrete example before treating the work as complete.\n7. Keep ci cd pipelines changes scoped to what was asked; avoid unrelated cleanup in the same pass.\n8. When multiple ci cd pipelines strategies are viable, name the trade-off explicitly rather than silently picking one.\n9. When ci cd pipelines decisions are ambiguous, favor the option that is easiest to reverse or extend later.\n10. Surface known limitations of a chosen ci cd pipelines approach instead of presenting it as complete when it is not.\n11. Prefer explicit, readable ci cd pipelines solutions over clever ones that are hard to verify.\n\nCOMMON PITFALLS TO AVOID\n- Do not treat a single passing example as full verification of ci cd pipelines correctness.\n- Do not silently drop ci cd pipelines edge cases to make an example look cleaner than the real system.\n- Avoid mixing unrelated ci cd pipelines concerns into a single change; keep each change focused.\n- Avoid introducing new ci cd pipelines abstractions before there is a second concrete use case that needs them.\n\nCLOSING NOTE\nRevisit these ci cd pipelines guidelines if the task's constraints change significantly during the work.",
  "token_count": 498,
  "metadata": {
    "subject": "ci_cd_pipelines",
    "aspect": "common_pitfalls",
    "source": "template"
  }
}